<h1> Items </h1>
<p><%= link_to 'New Task', new_item_path %></p>
<% @items.each do |item| %>
  <h2><%= item.title %></h2>
  <h2><%= item.done %></h2>
  <%= link_to 'Show Item', item_path(item) %>
<% end %>
  
